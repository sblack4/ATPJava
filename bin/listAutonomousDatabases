#!/usr/bin/env bash

#
# sblack.rocks 2018-09
# listAutonomousDatabases - no arguments

command="list"

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null && pwd )"

bash "$DIR/../java-runner.sh" "$command" "$@"
